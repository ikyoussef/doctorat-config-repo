spring:
  application:
    name: user-service

  datasource:
    url: jdbc:h2:mem:userdb
    driverClassName: org.h2.Driver
    username: sa
    password:
    platform: h2
  h2:
    console:
      enabled: true
      path: /h2-console

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/

jwt:
  secret: MaSuperCleSecreteUltraLonguePourHS512_ChangezEnProd
